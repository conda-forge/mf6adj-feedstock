{% set version = "1.4.0" %}

package:
  name: mf6adj
  version: {{ version }}

source:
  url: https://files.pythonhosted.org/packages/source/m/mf6adj/mf6adj-{{ version }}.tar.gz
  sha256: 6b5d7b9a8a724f8ae57625e510f92586efad99367dd3166fcfc9380e19fd7247

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}.*
    - hatchling
    - hatch-fancy-pypi-readme
    - pip
  run:
    - python >={{ python_min }}
    - numpy >=2.0
    - matplotlib-base >=3.5.0
    - pandas >=2.0.0,<4
    - flopy >=3.10.0
    - modflowapi >=1.0.0,<2
    - xmipy >=1.5.0
    - h5py >=3.0.0
    - scipy >=1.7.0
    - geopandas >=1.0.0

test:
  imports:
    - mf6adj
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}.*

about:
  home: https://pypi.org/project/mf6adj/
  summary: mf6adj is a Python package for adjoint-state sensitivity analysis with MODFLOW 6
  doc_url: https://md6adj.readthedocs.io
  license: CC0-1.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jdhughes-dev
    - jtwhite79
